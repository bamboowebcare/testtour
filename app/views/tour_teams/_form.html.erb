<%= simple_form_for(@tour_team) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :user %>
    <%= f.association :poule %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
